<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="cockatoo.enjizen.income">

    <application
            android:name=".IncomeApplication"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
    >
        <activity android:name=".ui.inoutcome.InOutComeActivity" android:screenOrientation="portrait">
        </activity>
        <activity android:name=".ui.password.PasswordActivity" android:screenOrientation="portrait"/>
        <activity android:name=".ui.account.AccountActivity"/>
        <activity
                android:name=".ui.main.MainActivity"  android:screenOrientation="portrait">
        </activity>
        <activity android:name=".ui.channel.VerifyChannelActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>