package cockatoo.enjizen.income.constant

enum class PasswordMode(val value: String) {
    CREATE("create password"),
    CHANGE("change password"),
    AUTHENTICATION("authentication with password")
}